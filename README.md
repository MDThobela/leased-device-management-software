# leased-device-management-software
The task is to build a Windows Forms application to manage lease contracts for cellular devices. When the main form loads, the logged-in user must be able to view all existing contract records, add new lease contracts with client details, and update selected contracts through an additional update form.
